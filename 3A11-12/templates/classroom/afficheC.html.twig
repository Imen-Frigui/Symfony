<h1>La liste des classes </h1>
<table border="1">
    <tr>
        <th>Id</th>
        <th>Nom</th>
        <th>Supprimer </th>
    </tr>
    {% for i in c %}
    <tr>
        <td>{{i.id}}</td>
        <td>{{i.name}}</td>
        <td><a href="{{path('suppC',{'id':i.id}) }}">Supprimer</a></td>

    </tr>
    {% endfor %}
</table>